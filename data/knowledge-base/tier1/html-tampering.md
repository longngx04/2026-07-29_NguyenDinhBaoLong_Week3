---
tier: 1
id: html-tampering
title: HTML Tampering client-side trust
tags: [example, html-tampering, client-side, validation]
---

# HTML Tampering

Chỉ validate giá trị trên HTML/JS (disabled field, hidden price). Attacker sửa DOM/request và bypass.

Mitigation: mọi kiểm soát nghiệp vụ phải ở server.
